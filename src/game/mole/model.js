var _ = require('lodash');
var Backbone = require('backbone');
var Model = require('src/common/model')


// Controls the logic for moles that spawn on the board
module.exports = Model.extend({
	defaults: {
		active: false
	},

	initialize: function() {},

});